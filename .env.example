VITE_API_BASE_URL=http://localhost:8000
VITE_RX_WS_URL=ws://localhost:5000/rx/ws
VITE_RX_CHUNK_DURATION=0.25
# NOTE: use_vortex is now fetched from the backend (GET /settings, sourced from config.yaml),
# not an env var. Set it in the backend config.yaml.
